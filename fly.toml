app = "cleanmind-api-germannaz"
primary_region = "gru"
kill_signal = "SIGTERM"
kill_timeout = "10s"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "npx prisma migrate deploy"
  strategy = "rolling"

[env]
  NODE_ENV = "production"
  PORT = "8080"
  APP_NAME = "CleanMind"
  BREVO_API_URL = "https://api.brevo.com/v3"
  BREVO_TIMEOUT_MS = "10000"
  MAIL_FROM = "cleanmind.support@gmail.com"
  MAIL_FROM_NAME = "CleanMind"
  SUPPORT_EMAIL = "germannaz_@hotmail.com"
  MFA_ISSUER = "CleanMind"
  FRONTEND_URL = "https://cleanmind.vercel.app"
  CORS_ORIGINS = "https://cleanmind.vercel.app"
  GOOGLE_CALLBACK_URL = "https://cleanmind.vercel.app/api/auth/google/callback"
  DISCORD_REDIRECT_URI = "https://cleanmind.vercel.app/api/notifications/discord/callback"
  DISCORD_OAUTH_SUCCESS_URL = "https://cleanmind.vercel.app/settings"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    grace_period = "15s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/api"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "512mb"
